export enum SeriesStatus {
    Active = "active",
    Paused = "paused",
    Canceled = "canceled"
}

export enum HistoryStatus {
    Pending = "pending",
    Verified = "verified",
    Ignored = "ignored"
}

export enum FrequencyUnit {
    Week = "week",
    Month = "month",
    Year = "year"
}